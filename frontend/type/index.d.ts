import type { Schedule as T_Schedule, ScheduleType as T_ScheduleType } from '@/type/schedule';
import type { ScheduleDateItem as T_ScheduleDateItem } from '@/type/scheduleDateItem';
import type { Subject as T_Subject } from '@/type/subject';

export namespace Type {
  export type Schedule = T_Schedule;
  export type ScheduleType = T_ScheduleType;
  export type ScheduleDateItem = T_ScheduleDateItem;
  export type Subject = T_Subject;
}
